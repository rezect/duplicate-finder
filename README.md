# 🔍 File Duplicate Finder

Консольная утилита на Go для поиска и анализа дублирующихся файлов в файловой системе с поддержкой параллельной обработки.

## 📋 Описание

File Duplicate Finder — это эффективный инструмент для рекурсивного сканирования директорий и обнаружения файлов-дубликатов на основе криптографических хеш-сумм. Программа помогает освободить дисковое пространство и навести порядок в файловой системе.

### Основные возможности

- 🗂️ **Рекурсивное сканирование** директорий с обходом всех вложенных папок
- ⚡ **Параллельная обработка** файлов для максимальной производительности
- 🔐 **Криптографические хеши** (MD5/SHA256) для точной идентификации дубликатов
- 📊 **Двухэтапная фильтрация**: сначала по размеру файла, затем по хеш-суммам
- 📈 **Детальная отчетность** с группировкой дубликатов и статистикой
- ⚙️ **Гибкая настройка** через флаги командной строки

## 🚀 Быстрый старт

### Установка

```bash
git clone https://github.com/yourusername/duplicate-finder.git
cd duplicate-finder
go build -o duplicate-finder ./cmd/finder
```

### Базовое использование

```bash
# Поиск дубликатов в текущей директории
./duplicate-finder

# Поиск в конкретной директории
./duplicate-finder -dir /path/to/directory

# Использование SHA256 вместо MD5
./duplicate-finder -algo sha256

# Игнорирование файлов меньше 1MB
./duplicate-finder -min-size 1048576
```

## 🛠️ Параметры командной строки

| Флаг | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `-dir` | string | `.` | Директория для сканирования |
| `-algo` | string | `md5` | Алгоритм хеширования (`md5`, `sha256`) |
| `-min-size` | int64 | `1` | Минимальный размер файла в байтах |
| `-workers` | int | `CPU*2` | Количество параллельных воркеров (1-1000) |
| `-debug` | bool | `false` | Режим отладки с детальным логированием |

### Примеры использования

```bash
# Поиск больших дубликатов с 8 воркерами
./duplicate-finder -dir ~/Downloads -min-size 10485760 -workers 8

# Максимальная точность с SHA256 и отладкой
./duplicate-finder -algo sha256 -debug

# Быстрый поиск дубликатов изображений
./duplicate-finder -dir ~/Pictures -min-size 100000
```

## 📊 Пример вывода

```
TotalFiles: 1523
FilesProcessed: 847/847

Вот краткий итог по найденным файлам:

Всего найдено групп файлов: 12

Группа 1 (a3d5e9f7b2c1d8e4f6a9b3c7d1e5f8a2)
Файлов: 3 шт.; Общий размер: 15728640
	- /home/user/Documents/backup/photo.jpg
	- /home/user/Pictures/photo.jpg
	- /home/user/Downloads/photo.jpg

Группа 2 (b4e6f1a8c3d9e2f5a7b1c4d8e3f6a9b2)
Файлов: 2 шт.; Общий размер: 8388608
	- /home/user/Videos/movie.mp4
	- /home/user/Videos/duplicates/movie.mp4
```

## 🏗️ Архитектура проекта

```
duplicate-finder/
├── cmd/
│   └── finder/
│       └── main.go           # Точка входа приложения
├── internal/
│   ├── cli/
│   │   └── cli.go           # Обработка аргументов командной строки
│   └── finder/
│       ├── hasher.go        # Вычисление хеш-сумм
│       ├── processor.go     # Параллельная обработка и сравнение
│       ├── reporter.go      # Формирование отчетов
│       └── scanner.go       # Сканирование файловой системы
├── go.mod
└── README.md
```

### Компоненты системы

#### Scanner (`scanner.go`)
- Рекурсивный обход директорий через `filepath.WalkDir`
- Первичная группировка файлов по размеру
- Фильтрация по минимальному размеру

#### Hasher (`hasher.go`)
- Вычисление MD5/SHA256 хеш-сумм
- Параллельная обработка через каналы
- Измерение времени выполнения в debug-режиме

#### Processor (`processor.go`)
- Управление пулом воркеров
- Группировка файлов по хеш-суммам
- Синхронизация через `sync.WaitGroup`

#### Reporter (`reporter.go`)
- Сортировка групп по размеру
- Форматированный вывод результатов
- Статистика по найденным дубликатам

## 🔧 Технический стек

- **Язык:** Go 1.19+
- **Стандартная библиотека:**
  - `crypto/md5`, `crypto/sha256` — криптографические хеш-функции
  - `path/filepath` — работа с путями и обход директорий
  - `sync` — синхронизация горутин
  - `flag` — парсинг аргументов командной строки
  - `io`, `os` — файловый ввод-вывод

## 💡 Ключевые особенности реализации

### Параллельная обработка

```go
// Пул воркеров для вычисления хеш-сумм
for range conf.Workers {
    go calculateHashParallel(conf, in, out)
}
```

Программа использует паттерн worker pool с настраиваемым количеством горутин (по умолчанию CPU×2) для эффективной обработки файлов.

### Двухэтапная фильтрация

1. **Быстрая фильтрация** по размеру файла (O(1) сравнение)
2. **Точная идентификация** через хеш-суммы (только для файлов одинакового размера)

Это значительно сокращает количество дорогостоящих операций хеширования.

### Управление ресурсами

```go
defer file.Close()
```

Использование `defer` гарантирует корректное закрытие файловых дескрипторов даже при возникновении ошибок.

## 📈 Производительность

- ⚡ Обработка **~1000 файлов/сек** на SSD (зависит от размера файлов)
- 💾 Оптимизированное использование памяти через потоковое чтение
- 🔄 Масштабируемость за счет параллельной обработки

## 🎯 Цели обучения

Проект создан для изучения и демонстрации:

- ✅ Работы с файловой системой в Go
- ✅ Применения криптографических хеш-функций
- ✅ Параллельного программирования (горутины, каналы)
- ✅ Эффективного использования структур данных (`map`)
- ✅ Идиоматических практик Go (defer, обработка ошибок)
- ✅ Архитектуры консольных приложений

## 🤝 Вклад в проект

Буду рад предложениям по улучшению! Не стесняйтесь открывать issues или отправлять pull requests.

## 📝 Лицензия

MIT License — свободное использование в образовательных целях.

---

**Автор:** rezect
**Контакты:** tg: @rezect